#name : unittest template
#contributor : sakito
# --
(dont-compile
  (when (fboundp 'expectations)
    (expectations
      (desc "$1")
      ;; (+ 1 3) が 3 になるべき。
      (expect 3
        (+ 1 2))
      $0
      )))
